# POC Config v1 - ML + ATR
# Tests if ATR filter adds value on top of ML

symbol: tBTCUSD
timeframe: 1h
start_date: "2024-06-01"
end_date: "2024-08-01"

components:
  - type: ml_confidence
    params:
      threshold: 0.5
  - type: atr_filter
    params:
      min_ratio: 1.0

description: "ML + ATR filter (min_ratio 1.0)"
